import {Dimensions} from 'react-native';

// refer to a video to understand this.
export const deviceHeight = Dimensions.get('window').height;
export const deviceWidth = Dimensions.get('window').width;
